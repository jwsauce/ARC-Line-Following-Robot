#include <Arduino.h>

void setup() {
    // Main loop is empty since we're running a fixed sequence in setup()
}

void loop() {
    // Main loop is empty since we're running a fixed sequence in setup()
}